**/*.md
# build output
**/dist
# generated: catalog.json is parsed from the JHU catalogue PDF, link-check.json
# is written by tools/check-course-links.mjs. Reformatting either by hand would
# be undone by the next regeneration.
aberatech.client/src/features/planner/data/*.json
# Hand formatted, and kept that way on purpose. Prettier cannot express this
# style: it has no brace-style option, so it will not put an opening brace on
# its own line, and it has never supported aligning values into a column.
# Running it over these files rewrote 88 lines of AppAppBar to change 12, and
# all 22 lines of Hero to change nothing at all.
aberatech.client/src/components/AppAppBar.tsx
aberatech.client/src/components/Hero.tsx
