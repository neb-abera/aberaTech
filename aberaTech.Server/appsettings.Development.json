{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "//": "The compose `db` service. A development password in a development file, for a port bound to loopback. The deployed connection string is a container app secret and is never committed.",
    "Scheduling": "Host=localhost;Port=5432;Database=scheduling;Username=scheduling;Password=scheduling"
  }
}
